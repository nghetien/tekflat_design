name: tekflat_design
description: Teko Flat Design.
version: 1.6.12-flutter3.22
homepage: https://github.com/nghetien/tekflat_design

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0 # Update lên là hẹo
  fluttertoast: ^8.2.2
  logger: ^2.0.2+1
  cached_network_image: ^3.3.0
  pull_to_refresh: ^2.0.0
  mask_text_input_formatter: ^2.5.0
  shimmer: ^3.0.0
  flutter_dotenv: ^5.1.0
  flutter_form_builder: ^9.3.0
  form_builder_validators: ^10.0.1
  collection: ^1.17.1 # Update lên là hẹo
  equatable: ^2.0.5
  shared_preferences: ^2.2.2
  flutter_svg: ^2.0.9
  top_snackbar_flutter: ^3.1.0
  reorderables: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter: